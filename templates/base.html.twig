<!DOCTYPE html>
<html lang="eng">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Weather!{% endblock %}</title>
    </head>
    <body>
        {% block body %}
        {{ forecast }}


    <div>
        <form method="get" action="{{ path('refresh') }}">
            <button>refresh</button>
        </form>
    </div>

        {% endblock %}
    </body>
</html>
